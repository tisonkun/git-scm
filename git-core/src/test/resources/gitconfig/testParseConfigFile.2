# Core variables
[core] ; Don't trust file modes
	filemode = false
    buttonOption
    buttonOptionAgain=
[bar.baz] ; deprecated subsection header
    foo = bar
    url = "https://example.com/git" ; quoted url
[url "git@example.com:"] ; url subsection header
    insteadOf = https://example.com/
